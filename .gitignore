node_modules
.next
out
coverage
.vercel
.env
.env.local
.env.*.local
.DS_Store
*.log

# Never publish private source photos, receipts, or reference-only images.
private/

# Private product data — contains paid prices and retailer names.
**/*.private.*
data/admin/product-image-cleanup-tracker.csv

# Approved product shots are added intentionally.
!public/products/approved/.gitkeep
